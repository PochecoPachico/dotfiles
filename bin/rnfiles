#!/bin/sh

if [ $# -ne 2 ]; then
	echo "Usage rnfiles [file extention] [prefix name]"
	exit 1
fi

count=1
for file in `\find . -maxdepth 1 -name "*.${1}" -type f`; do
	mv -v $file "${2}_${count}.${1}"
	count=$((count + 1))
done
